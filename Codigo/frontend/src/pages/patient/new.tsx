import Layout from "@/components/Layout";
import apiRoutes from "@/services/routes";
import Form from "@/templates/Patient/form";

const PatientForm = (props) => <Form {...props} />;

export default PatientForm;
